# 从单轮FAQ到多轮智能客服：我的第一个Agent项目改进复盘

> 作者：闪行星（fight1n）
>
> 项目：基于 RAG + Function Call 的智能客服 Agent
>
> 本文记录了我从第一个Agent项目POC出发，经过系统化改进后得到一个具备多轮对话、三级路由、熔断降级能力的完整项目的过程。这不是一个"从零到完美"的故事，而是一个"从能跑到好用"的工程实践记录。

---

## 一、项目背景与初始状态

### 1.1 为什么做这个项目

我是一名正在转型AI方向的开发者。在学习了RAG和Function Call的基本概念后，我需要一个项目来把这些知识串起来。智能客服是最经典的AI应用场景——业务逻辑清晰、技术栈覆盖面广、面试中容易展开讨论。

于是我做了一个AI智能客服Agent的POC，并用B站视频（BV1ETvTzsEgP）做了面试前的系统梳理。

### 1.2 初始项目的技术栈

| 组件 | 选型 |
|------|------|
| 大模型 | DeepSeek（兼容OpenAI接口） |
| 框架 | Java (Spring AI) + Python (LangChain) |
| 向量检索 | Embedding + KNN（L2距离） |
| 知识库 | FAQ文本，手动切分 |
| 意图识别 | 每次请求调LLM做分类 |

### 1.3 初始项目的状态

坦白说，初始项目只是"跑通了流程"：

- 单轮问答：用户输入 → FAQ向量匹配 → LLM生成回复，仅此而已
- 每次请求都调LLM做意图识别，延迟1-3秒
- 没有任何容错机制，LLM超时就报错
- 没有多轮对话，用户说"退款"这种模糊提问直接匹配失败
- Prompt硬编码在代码里，换模型要改几十处代码

### 1.4 评论区反馈

视频发布后，评论区给了我很多有价值的反馈。其中最高赞的评论（500赞）来自一位有实习经验的开发者，系统性地指出了六个改进方向：

1. 多轮对话与反问机制
2. 路由设计（不能全靠LLM判断意图）
3. 并发与性能（异步、并发、集群）
4. 调用链健壮性（熔断、重试）
5. 降级机制（最差结果不能是崩溃）
6. 工程化细节（Prompt版本管理、模型可替代性）

这些反馈促使我认真思考：哪些改进是务实可行的？哪些是过度工程化？

---

## 二、改进目标与方向筛选

### 2.1 务实评估

我没有试图一口气实现全部六个方向。以"单人开发、2-3周工期、无额外基础设施预算"为约束，我逐一评估了每个方向：

| 方向 | 工期 | 基础设施依赖 | 面试价值 | 决策 |
|------|------|-------------|---------|------|
| 多轮对话+反问 | 2周 | 低（仅需Redis） | 极高 | **核心推荐** |
| 三级路由 | 5天 | 零 | 高 | **核心推荐** |
| 并发+消息队列 | 2-3周 | 高（需MQ集群） | 中 | 精简：只做async |
| 熔断+重试 | 4天 | 零 | 高 | **核心推荐** |
| 降级链L0-L3 | 1周+ | L3需真实客服系统 | 中高 | 部分：只做L0-L2 |
| 工程化基础设施 | 1周 | 低 | 高 | **核心推荐（部分）** |

### 2.2 最终筛选结果

**核心推荐的四个方向**：

1. **D2 三级级联路由**——投入产出比最高，5天工期换80%的LLM调用减少
2. **D4 熔断+重试**——教科书级模式，工期最短，直接提升健壮性
3. **D6 模型抽象层+Prompt管理**——D4多模型切换的前置依赖
4. **D1 多轮对话+反问**——面试价值最高，直接补齐最明显的短板

**精简处理**：方向3只做FastAPI异步，不引入消息队列（Demo场景过度工程化）；方向5只做L0-L2降级，L3人工转接依赖真实业务系统无法实现。

### 2.3 关键决策：四个方向在同一项目实现

经过分析，这四个方向不是平行的独立功能，而是**自底向上的分层依赖关系**：

```
Layer 4  D1 对话编排  ← 依赖D2(路由)+D4(LLM调用)+D6(Prompt)
Layer 3  D2 路由层    ← L3路由器依赖D4(熔断保护)
Layer 2  D4 韧性层    ← 依赖D6(模型适配器才能多模型切换)
Layer 1  D6 基础设施  ← 无上游依赖，纯地基
```

拆分成多个独立项目反而会破坏天然耦合。正确的做法是：**同一项目，分层架构，按依赖顺序分阶段实现**。

---

## 三、各阶段优化措施与技术方案

### 3.1 阶段一：基础设施层（D6 + D4）

#### D6：模型抽象层

**问题**：原始项目中，DeepSeek的调用代码散落在各处。换模型（如切换到GLM）需要修改几十处代码。

**方案**：定义统一的 `ModelAdapter` 抽象接口，每个模型提供商实现自己的适配器：

```python
class ModelAdapter(ABC):
    async def generate(self, prompt: str, system: str = "") -> str
    async def stream_generate(self, prompt: str, system: str = "") -> AsyncIterator[str]
    async def structured_call(self, prompt: str, schema: dict) -> dict
```

由于DeepSeek、GLM、Qwen都兼容OpenAI接口，我写了一个 `OpenAICompatibleAdapter` 基类，三个适配器都继承它，几乎零额外代码。

**关键技术抉择**：

- **为什么不用LangChain的模型抽象**：原始项目中我对LangChain不够熟悉，手写实现反而更可控、更轻量。LangChain的抽象层太重，对于一个Demo项目来说引入了不必要的复杂度。
- **为什么用MockAdapter**：开发时没有API Key也能跑通全流程，这对调试和测试至关重要。MockAdapter通过正则匹配模拟LLM的槽位提取行为，让多轮对话流程可以在离线环境下完整测试。

#### D6：Prompt模板管理

**问题**：Prompt硬编码在Python代码中，修改Prompt需要重新部署。

**方案**：Prompt存为YAML文件，通过 `PromptManager` 加载和渲染：

```yaml
# prompts/slot_extraction_v1.yaml
template: |
  你是一个信息提取助手。请从用户输入中提取以下槽位信息。
  当前意图：{intent}
  需要提取的槽位：{slot_descriptions}
  用户输入：{user_input}
```

文件命名采用 `name_vN.yaml` 约定，天然支持版本管理（配合Git）。静态框架存YAML，动态变量运行时通过 `str.format()` 注入。

#### D4：熔断器

**问题**：LLM服务可能超时、限流或宕机，原始项目没有任何容错机制。

**方案**：实现经典的Circuit Breaker模式，状态机为 CLOSED → OPEN → HALF_OPEN → CLOSED：

- **CLOSED**：正常通行，记录成功/失败次数
- **OPEN**：连续失败5次或失败率>50%时触发，拒绝所有请求，等待30秒冷却
- **HALF_OPEN**：冷却后允许3个探测请求，成功则恢复CLOSED，失败则回到OPEN

**关键技术抉择**：

- **为什么自研而不用pybreaker库**：自研熔断器只有约80行代码，完全自控，面试时能讲清每个细节。引入第三方库反而增加了"你真的理解原理吗"的风险。
- **为什么每个模型提供商独立熔断**：DeepSeek和GLM的可用性是独立的。DeepSeek宕机不应该导致GLM也被熔断。独立熔断器让多模型切换更精准。

#### D4：指数退避重试

**方案**：`RetryPolicy` 实现指数退避 + 随机抖动：

```python
delay = min(base_delay * (2 ** attempt), max_delay)
delay *= 0.5 + random() * 0.5  # jitter
```

**关键决策**：熔断器开启时（`CircuitOpenError`）不重试——因为熔断意味着服务确实不可用，重试只会加重负担。

#### D4：多模型故障切换

**方案**：`ResilientLLMClient` 将熔断器+重试+多模型切换组合在一起：

```
调用DeepSeek → 熔断？→ 重试3次 → 仍失败？
                                    ↓
                              切换到GLM → 熔断？→ 重试3次 → 仍失败？
                                                          ↓
                                                    抛出AllProvidersDownError
```

用户完全无感知：DeepSeek宕机时，请求自动切到GLM，上层代码不需要任何改动。

---

### 3.2 阶段二：路由层（D2）

#### 问题分析

原始项目每次请求都调LLM做意图识别，延迟1-3秒。但仔细想想，大部分用户意图其实很明确——"我要退款"这种根本不需要LLM来判断。

#### 方案：三级级联路由

```
用户输入
  ├─ L1 规则路由（~1ms）── 正则关键词命中 → 直接返回
  │         │ 未命中
  ├─ L2 向量路由（~10ms）── Embedding余弦相似度 → 高置信度返回
  │         │ 低置信度
  └─ L3 LLM路由（~1-3s）── 大模型分类 → 返回（经D4熔断保护）
```

#### L1 规则路由

用正则表达式匹配最常见的关键词模式：

```python
RULES = {
    "refund": [r"(退款|退货|退钱)", r"(商品|东西).*(坏了|破损)"],
    "query_order": [r"(查询|查看).*(订单|物流)", r"订单号\s*[A-Z0-9]+"],
    "human": [r"(人工|客服|真人)"],
}
```

这一层能在1ms内处理约60-70%的请求。

#### L2 向量路由

**关键技术抉择——余弦相似度 vs L2距离**：

原始项目用L2距离做向量匹配，但弹幕中有观众指出"这里其实用余弦相似度更合适"。我查阅资料后确认：对于文本Embedding，余弦相似度只关注方向不关注模长，比L2距离更适合语义匹配。

L2路由为每个意图预计算"中心向量"（多个示例文本Embedding的平均），用户输入与各中心向量做余弦相似度，置信度≥0.75才采纳。

**纯Python实现**：由于环境中numpy安装超时，我用 `math` 模块手写了余弦相似度计算。代码只有十几行，性能对于Demo场景完全够用：

```python
def _cosine_similarity(a, b):
    dot = sum(x * y for x, y in zip(a, b))
    norm = math.sqrt(sum(x*x for x in a)) * math.sqrt(sum(y*y for y in b)) + 1e-8
    return dot / norm
```

#### L3 LLM路由

复杂意图（如"上个月那笔单子有点问题想看看"）交给LLM分类，调用经过D4的 `ResilientLLMClient` 保护。这一层只处理约5-10%的请求。

#### 效果

| 指标 | 改进前 | 改进后 |
|------|--------|--------|
| 平均路由延迟 | 1-3秒 | ~50ms（加权平均） |
| LLM调用量 | 100% | ~10%（仅L3） |
| LLM宕机时 | 路由崩溃 | L3失败降级为默认意图"faq" |

---

### 3.3 阶段三：对话编排层（D1）

#### 问题分析

原始项目只做单轮FAQ匹配。用户说"退款"时，系统不知道用户要退哪个订单、什么原因，直接去FAQ库匹配"退款"相关条目，返回通用回复。真实客服场景需要多轮对话来逐步明确用户意图。

#### 方案：Dialog State Tracking + 槽位填充 + 反问

**对话状态机**：

```
INTENT_DETECTION → SLOT_FILLING → CLARIFYING → RETRIEVING/EXECUTING → DONE
```

**槽位定义**：每个意图绑定若干必填槽位：

```python
INTENT_SLOTS_MAP = {
    "refund": [
        SlotDefinition("order_id", "订单编号", example="DD20240801001"),
        SlotDefinition("reason", "退款原因", example="商品损坏"),
    ],
    "query_order": [
        SlotDefinition("order_id", "订单编号"),
    ],
    "faq": [],  # FAQ不需要槽位
}
```

**多轮对话流程**：

1. 用户说"我要退款" → L1路由命中refund → 检查槽位 → order_id和reason都缺失 → 生成反问
2. 用户说"订单号DD001，商品损坏" → LLM提取槽位 → order_id=DD001, reason=商品损坏 → 槽位完整 → 执行退款流程
3. 返回"已为您发起退款申请：订单编号DD001..."

#### 关键技术抉择

**反问模板降级**：

当LLM熔断器处于OPEN状态时，反问生成会失败。我的解决方案是 `ClarificationService` 准备两套策略：

- LLM可用：用D6的Prompt模板 + D4的熔断保护调LLM生成自然语言反问
- LLM不可用：降级为静态模板反问（"请问您的订单号是多少？"），不依赖LLM

这确保了即使LLM完全不可用，多轮对话流程也不会崩溃。

**反问轮数限制**：

设置 `max_clarify_rounds=2`，超过后不再反问，降级为FAQ模糊匹配。避免用户始终不提供有效信息时陷入无限循环。

**D1与D2的职责划分**：

D1的 `_detect_intent()` 直接委托给D2的 `CascadeRouter.route()`，不自己实现意图识别。D1只管"什么时候需要做意图识别"（首轮/用户改话题时），D2管"怎么做"。

---

## 四、遇到的挑战及解决方案

### 挑战1：Mock环境下多轮对话测试

**问题**：开发时没有DeepSeek API Key，无法测试LLM槽位提取和反问生成。

**解决方案**：实现 `MockAdapter`，通过正则匹配模拟LLM行为：

- 检测到"槽位"关键词 → 从用户输入中用正则提取订单号（`DD\d+`）和退款原因关键词
- 检测到"反问"关键词 → 返回固定的反问模板
- 检测到"意图分类"关键词 → 返回 `{"intent": "faq"}`

关键细节：最初MockAdapter从整个Prompt文本中匹配正则，误将Prompt模板中的示例文本（"DD20240801001"）当成用户输入提取。修复方法：只从"用户输入："之后的文本段中提取，避免示例干扰。

### 挑战2：熔断器测试中的失败率阈值

**问题**：测试熔断器时，设置 `min_requests=1`，导致2次失败就触发熔断（失败率100% > 50%阈值），与预期的"连续失败3次触发"不一致。

**解决方案**：`min_requests` 控制何时开始计算失败率。设置为10意味着：只有总请求数≥10时才看失败率，否则只看连续失败数。这避免了少量请求时失败率波动过大。

### 挑战3：向量路由的置信度阈值

**问题**：Mock模式下用确定性伪随机向量做余弦相似度，得分普遍在0.2左右，远低于0.75的阈值，导致L2路由始终返回None。

**解决方案**：区分真实模式和Mock模式的阈值。真实模式用0.75确保高准确率；Mock模式直接返回最高分意图（用于功能测试，不验证准确率）。

### 挑战4：numpy安装超时

**问题**：开发环境中 `pip install numpy` 下载超时，无法安装。

**解决方案**：向量路由改用纯Python实现（`math` 模块 + 列表），不依赖numpy。余弦相似度计算只有十几行代码，性能对Demo场景完全够用。这也降低了项目的安装门槛——只需要 `PyYAML` 和 `openai` 两个依赖。

### 挑战5：Prompt模板中的花括号冲突

**问题**：槽位提取Prompt中包含JSON格式示例 `{"slot_name": "value"}`，与Python的 `str.format()` 变量注入冲突——`{slot_name}` 会被当成变量名。

**解决方案**：在YAML模板中，字面花括号用双写转义：`{{"slot_name": "value"}}`。`str.format()` 会将 `{{` 渲染为 `{`，`}}` 渲染为 `}`，变量注入不受影响。

---

## 五、最终成果

### 5.1 项目结构

```
customer-service-agent/
├── config/app.yaml                    # 配置中心
├── prompts/                           # Prompt模板（YAML + Git版本管理）
├── src/
│   ├── models/adapter.py              # D6: 模型抽象层
│   ├── prompts/manager.py             # D6: Prompt管理器
│   ├── resilience/                    # D4: 熔断+重试+多模型切换
│   ├── routing/                       # D2: 三级级联路由
│   ├── dialog/                        # D1: 多轮对话+反问
│   ├── rag/service.py                 # RAG检索+生成
│   └── app.py                         # FastAPI入口
├── tests/run_tests.py                 # 9个测试（无需API Key）
└── requirements.txt                   # 仅5个依赖
```

### 5.2 测试覆盖

9个测试全部通过，覆盖四层架构的每个模块：

| 测试 | 验证内容 |
|------|---------|
| test_mock_adapter | Mock适配器离线模式 |
| test_prompt_manager | YAML模板加载+变量注入 |
| test_rule_router | L1规则路由（退款/查单/转人工） |
| test_vector_router | L2向量路由（余弦相似度） |
| test_cascade_router | 三级级联（L1→L2→default） |
| test_circuit_breaker | 熔断器状态机（CLOSED→OPEN→HALF_OPEN→CLOSED） |
| test_retry | 指数退避重试 |
| test_dialog_context | 槽位填充检测 |
| test_full_dialog_flow | 完整多轮对话流程 |

### 5.3 改进前 vs 改进后

| 维度 | 改进前 | 改进后 |
|------|--------|--------|
| 意图识别延迟 | 每次调LLM，1-3秒 | 90%请求50ms内完成 |
| LLM故障处理 | 直接报错 | 熔断→重试→切GLM→降级 |
| 多轮对话 | 不支持 | 支持2-3个核心意图的槽位填充+反问 |
| 换模型 | 改几十处代码 | 改config.yaml一行 |
| Prompt管理 | 硬编码 | YAML文件+Git版本管理 |
| 并发 | 同步阻塞 | FastAPI async |
| 测试 | 无法离线测试 | Mock模式9个测试全通过 |

---

## 六、反思

### 6.1 做对了的事

1. **没有贪多**：6个方向只选了4个核心+2个精简，避免过度工程化。消息队列和人工转接对Demo项目来说确实是陷阱。

2. **Mock优先**：从一开始就设计MockAdapter，让开发全过程不依赖API Key。这极大地提升了开发效率和测试覆盖率。

3. **分层架构**：四层依赖关系清晰，每层可独立测试。修改一层不影响其他层，调试时能快速定位问题。

4. **务实的面试策略**：在面试中明确说清楚"做了什么"和"没做什么"。不说"支持高并发"（只做了async没有MQ），不说"有完整降级链"（L3未实现）。

### 6.2 可以做得更好的事

1. **向量路由的Mock不够真实**：当前用确定性伪随机向量，得分普遍在0.2左右。如果用真实的Embedding模型，L2路由的准确率会高很多。下一步可以接入真实Embedding API。

2. **槽位提取的Mock用正则**：MockAdapter用正则模拟槽位提取，只能处理"DD001"这种固定格式。真实场景中用户可能说"订单号是DD001"或"单号DD001"等多种表达，需要LLM来理解。

3. **没有对话历史压缩**：当前DialogContext保留最近10条历史，多轮对话过长时会导致Prompt超出token限制。应该实现对话历史摘要/压缩。

4. **知识库只有关键词匹配**：RAG模块目前用简单的关键词匹配做检索，没有用真正的向量检索。下一步应该接入向量数据库（如Chroma或Milvus）。

### 6.3 面试中如何讲这个项目

我会在面试中这样描述：

> "我的项目是一个基于RAG+Function Call的智能客服Agent。第一版只做了单轮FAQ匹配，后来根据反馈做了系统改进：引入三级级联路由把90%的意图识别延迟从秒级降到毫秒级；加了熔断器和多模型切换保证LLM故障时不崩溃；实现了多轮对话的槽位填充和反问机制；用模型抽象层和Prompt管理做了工程化规范。
>
> 没有做的是消息队列和人工转接——前者对Demo是过度工程化，后者依赖真实业务系统。这些是我在评估后主动选择暂缓的。"

这个回答展示了三件事：技术能力（做了什么）、工程判断力（选择不做什么）、诚实态度（明确说清楚边界）。

---

## 七、技术选型总结

| 组件 | 选型 | 选择理由 |
|------|------|---------|
| Web框架 | FastAPI | 原生async支持，SSE流式输出 |
| LLM | DeepSeek | 兼容OpenAI接口，成本低 |
| 备用LLM | GLM-4-flash | 工具调用能力强，免费额度 |
| 向量计算 | 纯Python (math) | 不依赖numpy，降低安装门槛 |
| 熔断器 | 自研 (~80行) | 完全自控，面试可讲清细节 |
| 重试 | 自研 | 指数退避+jitter，不依赖tenacity |
| Prompt管理 | YAML + Git | 轻量，天然版本管理 |
| 配置 | YAML + 环境变量 | 敏感信息不进代码 |
| 测试 | asyncio.run | 无需pytest，直接运行 |

---

> **最后想说的话**：这个项目不完美，但它真实。从"跑通了流程"到"能扛住故障、能多轮对话、能快速切换模型"，每一步改进都有明确的问题驱动和技术权衡。这比一个看似完美但说不清为什么的Demo更有价值。
>
> 感谢B站评论区所有给过我反馈的朋友们，特别是一位叫"-我七起氏-"的评论者，TA的那条500赞评论是这个项目改进的起点。
